// FilterStyles.js
import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  filterContainer: {
    padding: 10,
    backgroundColor: '#fff',
    borderRadius: 8,
    marginBottom: 10,
  },
  picker: {
    height: 50,
    width: '100%',
  },
});

export default styles;
